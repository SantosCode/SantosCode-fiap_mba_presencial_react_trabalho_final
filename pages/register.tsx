import React, { ReactElement } from 'react';
import RegisterController from "../src/scenes/register/register-controller";

const Login = (): ReactElement => (
  <RegisterController />
)

export default Login;