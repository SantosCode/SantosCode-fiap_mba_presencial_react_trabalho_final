import React, { ReactElement } from 'react'
import FavoritesController from '../src/scenes/favorites/favorites-controller'

const Home = (): ReactElement => (
  <FavoritesController />
)

export default Home