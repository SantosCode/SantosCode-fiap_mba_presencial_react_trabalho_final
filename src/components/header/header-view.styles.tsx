import { CSSProperties } from "react"

export const styles: {[key: string]: CSSProperties} = {
  headerTrailingContent: {
    alignItems: "center",
    display: "flex",
    flexDirection: "row",
    padding: "0 0 0"
  }
}